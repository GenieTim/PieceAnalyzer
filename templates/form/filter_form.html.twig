{{ form_start(form) }}
<div class="container">
    <div class="row">
        <div class="col">
            {{ form_row(form.color) }}
        </div>
        <div class="col">
            {{ form_row(form.category) }}
        </div>
        <div class="col-md-auto">
            <label>Filter
                {{ form_row(form.search) }}
            </label>
        </div>
    </div>
</div>
{{ form_end(form) }}