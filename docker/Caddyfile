{
    frankenphp
}

{$SERVER_NAME::80} {
    root * /app/public
    encode zstd gzip

    # Cache static frontend assets
    @assets path /build/*
    header @assets Cache-Control "public, max-age=31536000, immutable"

    php_server
}
